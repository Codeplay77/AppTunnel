<Window x:Class="AppTunnel.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:AppTunnel.Views"
        Title="AppTunnel" Height="600" Width="960" Icon="favicon.ico"
        Background="{DynamicResource CorFundo}" Foreground="{DynamicResource CorTextoPrimario}">
    <TabControl Margin="4">
        <TabItem Header="Instâncias">
            <views:InstanciasView DataContext="{Binding Instancias}"/>
        </TabItem>
        <TabItem Header="Conexões">
            <views:ConexoesView DataContext="{Binding Conexoes}"/>
        </TabItem>
        <TabItem Header="Proxies">
            <views:ProxiesView DataContext="{Binding Proxies}"/>
        </TabItem>
        <TabItem Header="Log">
            <views:LogView DataContext="{Binding Log}"/>
        </TabItem>
    </TabControl>
</Window>
